"""Root pytest configuration and shared fixtures.

Kept intentionally empty for now: no cross-cutting fixtures are needed by
the Phase 1 scaffolding tests. Later phases (domain, ports, adapters) will
add fixtures here (or in tests/fixtures/) as real test needs emerge, per
TDD discipline — fixtures are added when a test needs them, not before.
"""
